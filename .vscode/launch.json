{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Mimora: npm run dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm.cmd run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
